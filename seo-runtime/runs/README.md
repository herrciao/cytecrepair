# Run Records

Create one file per weekly Runtime cycle from `../templates/run-template.md`. A run is an append-only operational record: correct mistakes transparently rather than replacing historical measurements.
